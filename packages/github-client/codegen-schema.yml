schema:
  - https://api.github.com/graphql:
      headers:
        Authorization: Bearer ${GITHUB_TOKEN}
        User-Agent: "@giselle-sdk/github-client"
generates:
  ./schema.graphql:
    plugins:
      - schema-ast
